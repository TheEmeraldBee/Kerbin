pub fn load(api) {
    // Rust
    api.grammar.register_extension("rs", "rust");

    // Python
    api.grammar.register_extension("py", "python");

    // Rune
    api.grammar.register_extension("rn", "rune");
}
