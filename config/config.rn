mod keybindings;
mod langs;
mod theme;

mod modeline;

pub fn load(api) {
    langs::load(api);
    theme::load(api);
    keybindings::load(api);

    modeline::load(api);
}

pub fn update(api) {
    modeline::update(api);
}
