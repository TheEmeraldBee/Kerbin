[package]
name = "kerbin-command-lang"
version.workspace = true
edition.workspace = true

[dependencies]
thiserror.workspace = true
async-trait.workspace = true
serde = { version = "1.0.228", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
